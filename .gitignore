# The encryption passcode lives here and must NEVER be committed.
secret/
# local dev server log
server.log
# python cache
__pycache__/
*.pyc
# temp validation scratch (safe to delete)
_validate_publish.py
